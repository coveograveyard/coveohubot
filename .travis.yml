sudo: required

language: node_js

services:
  - docker

before_install:
- docker build -t coveo/coveohubot .
- docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
- docker push coveo/coveohubot

